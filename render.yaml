services:
  - type: web
    name: world-cup-stock-api
    env: python
    plan: free
    pythonVersion: 3.11.9
    buildCommand: pip install -r api_service/requirements.txt
    startCommand: uvicorn api_service.app:app --host 0.0.0.0 --port 10000
    autoDeploy: true
